---
description: Non-negotiable repository rules for all work in this Chrome extension monorepo.
alwaysApply: true
---

For meaningful work, recover repository state before editing:
- @CURRENT.md and its full active-work table
- target GitHub Issue and latest valid STATE/OWNER/REVIEW_MODE transition
- @docs/COLLABORATION.md
- @AGENTS.md

Repository-wide references:
- @docs/ARCHITECTURE.md
- @docs/SECURITY.md
- @docs/QA.md
- @docs/STORE_POLICY.md
- @docs/DEVELOPMENT.md
- @docs/DESIGN_SYSTEM.md
- @docs/CURSOR.md
- @docs/GITHUB_AGENT_SETUP.md

Mandatory behavior:
- GitHub is the operational collaboration hub; do not use chat history or local scratch state as a substitute for durable task state.
- ChatGPT is optional. Cursor-only execution is valid when the GitHub task contract is followed.
- Check all RUNNING tasks for declared write-scope collisions before editing.
- Preserve the multi-extension monorepo boundary: products live under `apps/<slug>`.
- Chrome Manifest V3, TypeScript, WXT, pnpm, Vitest, and Playwright are the repository defaults.
- Do not broaden permissions, data collection, external transmission, or product scope without documenting it first.
- Never add remote executable code, `eval`, `new Function`, secrets, or session material.
- Treat background service workers as ephemeral and third-party page content as untrusted.
- Keep unrelated extensions unchanged.
- Use the shared design system for extension-owned UI.
- Run `pnpm verify:repo` and all relevant QA gates before declaring implementation complete.
- When changing GitHub task state from Cursor, `pnpm agent:check` must pass first.
- Follow the Issue ownership/state/review-mode protocol in `docs/COLLABORATION.md`.
- `REVIEW_MODE: SELF` requires a distinct second review pass; do not treat self-review as skipped review.
- Unresolved material decisions must move to `DECISION_NEEDED` instead of being guessed.
- If repository rules conflict with an app-local convenience, repository rules win unless the app SPEC explicitly records an approved exception.
